import React from 'react';
import { HeaderView, HeaderTitle } from '../styles/style';

export const Header = () => {
  return (
    <HeaderView>
      <HeaderTitle>Tasks</HeaderTitle>
    </HeaderView>
  )
}
